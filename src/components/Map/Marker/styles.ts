import { createStyles } from "@material-ui/core";

const styles = createStyles({
  root: {
    position: "absolute"
  },
  cardMedia: {
    margin: "auto",
    width: 150,
    height: 150
  }
});

export default styles;
