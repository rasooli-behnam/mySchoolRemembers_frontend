import { createStyles } from "@material-ui/core";

const styles = createStyles({
  buttonAvailable: {
    backgroundColor: "#c4f296",
    fontWeight: "bold"
  },
  buttonNotAvailable: {
    backgroundColor: "#fccd9c",
    fontWeight: "bold"
  }
});

export default styles;
